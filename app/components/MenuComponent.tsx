import Image from 'next/image';
import { useState } from 'react';
import menuGif from "@/public/gif/menu.gif";

type Gif = boolean | string;

export default function IconComponent() {
    const [open, setOpen] = useState(false);

    return (
        
    );
}